# Agent Instructions

## Pre-commit

Run `bun check` before committing. It handles type checking, linting, and formatting — auto-fixing what it can. Any issues that can't be auto-fixed must be resolved before committing.
